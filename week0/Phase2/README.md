🔹 Data Processing Steps (Simple Explanation)
Cleaned the dataset by removing missing values using dropna()
Converted the amount column into integer format using cast() for accurate calculations
Used groupBy() and agg() to perform calculations on grouped data
Calculated important metrics like total spending and average values
Sorted the data using orderBy() to identify top records
Used limit() to extract top results (like top customers)
Applied join() to combine multiple datasets (customers and sales)
Identified customers who have no orders using join logic
Filtered customers who placed more than one order
